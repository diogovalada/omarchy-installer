[package]
name = "omarchy-media-writer"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Platform-neutral, bounded and fully verified image writer for Omarchy Setup"

[lib]
path = "src/lib.rs"

[lints.rust]
unsafe_code = "forbid"
