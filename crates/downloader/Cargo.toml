[package]
name = "omarchy-downloader"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Offline-testable verified artifact cache primitives for Omarchy Setup"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
thiserror = "2.0"

[dev-dependencies]
tempfile = "3.20"
