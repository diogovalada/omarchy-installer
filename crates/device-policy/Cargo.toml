[package]
name = "device-policy"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Pure, fail-closed target classification for Omarchy Installer"
publish = false

[dependencies]

[dev-dependencies]
proptest = "1.7"

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
